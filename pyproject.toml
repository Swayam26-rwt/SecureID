[project]
name = "secureid"
version = "2.0.0"
description = "SecureID — High-performance offline facial recognition and liveness detection engine with advanced ML signal processing."
requires-python = ">=3.9"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Swayam Rawat", email = "swaymrawat862@gmail.com" }]
keywords = ["biometrics", "facial-recognition", "liveness-detection", "edge-computing", "computer-vision", "machine-learning", "secureid"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Security",
    "Topic :: Scientific/Engineering :: Image Recognition",
]
dependencies = []

[project.scripts]
secureid = "biometric_engine.cli:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "mypy>=1.9.0",
    "ruff>=0.3.0",
]

[project.urls]
Homepage = "https://github.com/Swayam26-rwt/SecureID"
Repository = "https://github.com/Swayam26-rwt/SecureID.git"
Documentation = "https://github.com/Swayam26-rwt/SecureID#readme"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["biometric_engine*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.mypy]
python_version = "3.10"
strict = true
warn_unused_configs = true
